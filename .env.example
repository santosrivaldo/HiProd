
# Database Configuration - PostgreSQL
# Opção 1: URL completa (recomendado)
DATABASE_URL=postgresql://username:password@host:port/database_name

# Opção 2: Variáveis individuais (fallback)
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
DB_HOST=your_host
DB_PORT=5432

# JWT Configuration
JWT_SECRET_KEY=your-very-secure-secret-key-change-this-in-production

# Para usar localmente, copie este arquivo para .env e configure suas credenciais
